language: java
jdk: oraclejdk8
sudo: required

addons:
  apt:
    packages:
    - glpk

before_install:
  chmod +x maven_script.sh

install:
  true

script:
  ./maven_script.sh

after_success:
  echo $SUCCESSMESSAGE

after_failure:
  echo $ERRORMESSAGE

notifications:
  email: false